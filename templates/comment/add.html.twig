{% extends 'base.html.twig' %}

{% block title %}Ajouter un commentaire{% endblock %}

{% block body %}
<div class="cont">
    <h1 class="title-h1 title-center">Ajouter un commentaire à {{ item.name }}</h1>
    <div class="form">
        {{ form_start(add_comment_form) }}
            <div class="form-group">
                {{ form_row(add_comment_form.message) }}
            </div>
            <div class="form-submit">
                {{ form_row(add_comment_form.submit) }}
            </div>
        {{ form_end(add_comment_form) }}
    </div>
</div>
{% endblock %}