{% extends 'base.html.twig' %}

{% block title %}Ajouter un item{% endblock %}

{% block body %}
<div class="cont">
    <h1 class="title-h1 title-center">Ajouter un objet à {{ album.name }}</h1>
    <div class="form">
        {{ form_start(add_item_form) }}
            <div class="form-group">
                {{ form_row(add_item_form.name) }}
            </div>
            <div class="form-group">
                {{ form_row(add_item_form.description) }}
            </div>
            <div class="form-submit">
                {{ form_row(add_item_form.submit) }}
            </div>
        {{ form_end(add_item_form) }}
    </div>
</div>
{% endblock %}